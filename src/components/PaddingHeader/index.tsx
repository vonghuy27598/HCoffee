import React from 'react';
import {View} from 'react-native';
import {styles} from './styles';

const PaddingHeader = () => {
  return <View style={styles.spaceHeader} />;
};

export default PaddingHeader;
