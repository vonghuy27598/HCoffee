export interface permisstionLocationType {
  android: {
    PERMISSIONS_ANDROID_ACCESS_BACKGROUND_LOCATION: string;
    PERMISSIONS_ANDROID_ACCESS_FINE_LOCATION: string;
  };
  ios: {
    PERMISSIONS_IOS_LOCATION_ALWAYS: string;
    PERMISSIONS_IOS_LOCATION_WHEN_IN_USE: string;
  };
}
